query FindAllCategories {
  findAllCategories {
    id
    name
    slug
    imageUrl
    parentId
    children {
      id
      name
      slug
      imageUrl
      createdAt
    }
    createdAt
  }
}
