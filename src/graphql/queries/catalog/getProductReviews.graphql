query GetProductReviews($productId: String!) {
  getProductReviews(productId: $productId) {
    total
    avgRating
    data {
      id
      userId
      rating
      title
      text
      isVerified
      createdAt
    }
  }
}
