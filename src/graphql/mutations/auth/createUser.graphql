mutation CreateUser($data: CreateUserInput!) {
  createUser(data: $data) {
    user {
      id
      username
      email
      avatar
      role
      isEmailVerified
      isTotpEnabled
      isVerified
      createdAt
    }
    message
  }
}
